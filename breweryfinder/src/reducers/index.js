/***********************made initial state to put in the store ********************************************/

export const appReducer = (state, action) => {
    switch(action.type){
        default:
            return state;
    }
}